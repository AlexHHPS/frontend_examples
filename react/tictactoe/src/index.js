import React from 'react';
import ReactDOM from 'react-dom/client';
import './beatiful_solution/App.css';
import App from './beatiful_solution/App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <React.StrictMode>
        <App/>
    </React.StrictMode>
);
